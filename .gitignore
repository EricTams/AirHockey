# Incoming art drops — staging only. Normalized copies live in public/assets/.
ContentDrop-*/

node_modules/
dist/
.DS_Store
*.log

# Local art inspection log (dev tooling; contains copies of drop art)
.artlog/
